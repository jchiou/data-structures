// A stack takes the last item in as the first item out, while a queue takes the first item in as the first item out. A stack would be used for storing the return values of a recursive function, while a queue would be used for assigning values to variables based off of which variables come first. 
